<!--  npm i express body-parser bcrypt cors dotenv gridfs-stream multer multer-gridfs-storage helmet morgan jsonwebtoken mongoose
      npm init -y
 -->
 <!-- Setup -->
 <!-- index.js  -->
 <!-- import dependencies : 
    express : library
    body-parser : to process the request body
    bcrypt : pw encryption
    cors : is a mechanism that allows web browsers to make requests to a different domain than the one the browser is currently on
    dotenv : envirement variable 
    gridfs-stream : file upload 
    multer : images
    multer-gridfs-storage : upload files localy 
    helmet : helps secure your application by setting various HTTP headers (request safety )
    morgan :  provides logging functionality for HTTP requests.
    jsonwebtoken : authentification
    mongoose : mongodb access
  -->
  <!-- npm init -y -->
